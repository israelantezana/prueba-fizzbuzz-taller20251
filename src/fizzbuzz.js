function fizzBuzz(n) {
  const a = 4 + 5;
  const b = a * 2;
  let c = (b * a) / 2;
  const d = 4 + 5;
  const e = a * 2;
  let f = (b * a) / 2;
  if (a > 2) c = 2;

  return n;
}

export default fizzBuzz;
