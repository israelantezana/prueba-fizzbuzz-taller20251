//import sumar from "./sumador.js";

describe("Fizzbuzz", () => {
  it("retornar el numero si no sigue una regla", () => {
    expect(fizzbuzz("1")).toEqual(1);
  });
});

function fizzbuzz(n) {
  return 1;
}
